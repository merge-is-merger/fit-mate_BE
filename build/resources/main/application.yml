spring:
  datasource:
    url: ${INSTANCE_CONNECTION_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver
#  datasource:
#    url: jdbc:mysql://localhost:3306/fitmate_test_db
#    username: root
#    password: j2nny0620*
#    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect

cloud:
  aws:
    credentials:
      access-key: AKIA4MTWNPIB4WL2LASUF
      secret-key: mIbS3t+wOOteizfsvZ0NN/9VezjCyFMk+1H+2Iu3
    region:
      static: ap-northeast-2
    s3:
      bucket: my-unique-bucket-profile-image


logging:
  level:
    org:
      springframework:
        security: DEBUG
        web: DEBUG
        hibernate: ERROR
